<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TodoSQLite" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/bluegreen</item>
        <item name="colorPrimaryVariant">@color/bluegreen</item>
        <item name="android:statusBarColor">@color/bluegreen</item>
    </style>

    <style name="Theme.TodoSQLite" parent="Base.Theme.TodoSQLite" />
</resources>